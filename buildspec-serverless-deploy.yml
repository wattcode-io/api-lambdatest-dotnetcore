version: 0.2

phases:
  install:
    commands:
      - printenv
      - npm install
      - npm install -g serverless

  build:
    commands:
      - pwd
      - ls
      - serverless deploy -v --package deploy-package.zip

artifacts:  
  base-directory: ./
  files:
    - '**/*'